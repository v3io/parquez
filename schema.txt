id string,
link_id string,
direction string,
qos_id bigint,
pe_add string,
pe_name string,
interface string,
util double,
max_bw bigint,
curr_cost1 bigint,
curr_cost2 bigint,
util_per double,
link_status string,
pkt_rate bigint,
latency double,
avg_latency bigint,
base_avg_latency bigint,
alarm bigint,
time_stamp bigint,
total_score bigint
